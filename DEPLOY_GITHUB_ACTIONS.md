# Деплой через GitHub Actions

Автоматический деплой на GitHub Pages при каждом push в ветку `main`.

## ✅ Что уже настроено:

- ✅ GitHub Actions workflow создан (`.github/workflows/deploy.yml`)
- ✅ Homepage в package.json: `https://Vladshmalii.github.io/bot_new_year`
- ✅ Git репозиторий инициализирован

## 🚀 Шаги для первого деплоя:

### 1. Загрузите код на GitHub:

```bash
git add .
git commit -m "Add GitHub Actions workflow"
git push -u origin main
```

### 2. Включите GitHub Pages в настройках репозитория:

1. Перейдите на https://github.com/Vladshmalii/bot_new_year/settings/pages
2. В разделе **Source** выберите:
   - **Source**: `GitHub Actions` (не ветка!)
3. Нажмите **Save**

**Важно:** Выберите именно **GitHub Actions**, а не ветку `gh-pages`!

### 3. Запустите workflow вручную (опционально):

1. Перейдите на https://github.com/Vladshmalii/bot_new_year/actions
2. Выберите workflow "Deploy to GitHub Pages"
3. Нажмите **Run workflow** → **Run workflow**

Или просто сделайте любой commit и push - workflow запустится автоматически!

### 4. Готово! 🎉

Через 1-2 минуты сайт будет доступен по адресу:
**https://Vladshmalii.github.io/bot_new_year**

## 📝 Как это работает:

- При каждом **push в ветку `main`** автоматически запускается workflow
- Workflow собирает React приложение
- Результат автоматически деплоится на GitHub Pages
- Не нужно запускать `npm run deploy` вручную!

## 🔄 Обновление сайта:

Просто сделайте commit и push:

```bash
git add .
git commit -m "Update app"
git push
```

GitHub Actions автоматически задеплоит изменения!

## 📊 Мониторинг деплоя:

- Перейдите на https://github.com/Vladshmalii/bot_new_year/actions
- Там вы увидите все запуски workflow
- Зеленый значок ✅ = успешный деплой
- Красный ❌ = ошибка (нажмите на workflow, чтобы увидеть детали)

## ⚠️ Важно:

1. **Source должен быть GitHub Actions**, а не ветка!
2. После первого push подождите 2-3 минуты
3. Если деплой не работает, проверьте:
   - Настройки Pages (должен быть выбран GitHub Actions)
   - Логи в Actions (Actions → Deploy to GitHub Pages → последний запуск)

## 🆚 Преимущества перед gh-pages:

- ✅ Автоматический деплой при каждом push
- ✅ Не нужно запускать команды вручную
- ✅ История деплоев в GitHub Actions
- ✅ Логи и отладка прямо в GitHub
- ✅ Не засоряет основную ветку веткой gh-pages

