# Настройка data.json для GitHub Pages

## ✅ Что уже настроено:

1. **Файл `data.json` находится в `frontend/public/data.json`**
   - React автоматически копирует все файлы из `public` в корень `build` при сборке
   - Файл будет доступен как `/bot_new_year/data.json` на GitHub Pages

2. **Код обновлен для правильных путей:**
   - Используется `process.env.PUBLIC_URL` для автоматического определения базового пути
   - На GitHub Pages: `/bot_new_year/data.json`
   - Локально: `/data.json`

3. **`homepage` в `package.json` настроен:**
   ```json
   "homepage": "https://Vladshmalii.github.io/bot_new_year"
   ```

## 🚀 Как это работает:

### При сборке (`npm run build`):
1. React Scripts читает `homepage` из `package.json`
2. Устанавливает `PUBLIC_URL = "/bot_new_year"`
3. Копирует `public/data.json` → `build/data.json`
4. Все пути в коде автоматически используют `PUBLIC_URL`

### На GitHub Pages:
- Файл будет доступен по адресу: `https://Vladshmalii.github.io/bot_new_year/data.json`
- Код автоматически загрузит его при запуске приложения

## 📝 Обновление данных:

1. Отредактируйте `frontend/public/data.json`
2. Закоммитьте изменения:
   ```bash
   git add frontend/public/data.json
   git commit -m "Update game data"
   git push
   ```
3. GitHub Actions автоматически пересоберет и задеплоит сайт
4. Через 1-2 минуты новые данные будут доступны на сайте

## ⚠️ Важно:

- **Файл `data.json` будет публично доступен** на GitHub Pages
- Все данные в нем видны в исходном коде репозитория
- Если нужна приватность, рассмотрите использование переменных окружения или API

## 🔍 Проверка:

После деплоя проверьте:
1. Откройте: `https://Vladshmalii.github.io/bot_new_year/data.json`
2. Должен открыться JSON файл с данными
3. В консоли браузера не должно быть ошибок загрузки

